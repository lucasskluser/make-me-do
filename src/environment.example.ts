export default class Environment {
    public static readonly JWT_SECRET: string = 'your-jwt-secret';
    public static readonly JWT_ISSUER: string = 'your-jwt-issuer';
    public static readonly JWT_EXPIRATION: string = '15m';
}